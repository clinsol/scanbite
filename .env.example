# Free key from https://api.data.gov/signup/ - without it the app falls
# back to USDA's public DEMO_KEY (rate-limited: 30 req/hour, 50/day per IP),
# which still works, it's just easy to exhaust with real use.
VITE_USDA_API_KEY=
